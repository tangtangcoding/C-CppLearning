#include <stdio.h>
#define SIZE 5001
int main() {
    char ch[SIZE];
    while (scanf("%c", &ch) != EOF) { // 注意 while 处理多个 case
        // 64 位输出请用 printf("%lld") to 
        printf("%d\n", a + b);
    }
    return 0;
}