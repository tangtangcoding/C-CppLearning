#include <stdio.h>
#include <string.h>

boolean containsDuplicate(int* nums, int numsSize) {
    for (int i=0;i<numsSize;++i) {
        printf("%d ",&nums[i]);
    }
    return 1;
}

int main()
{
    char str[1000];
    int a=0,i=0;

    while(scanf("%s",str) != EOF)
    {}
    a=strlen(str);
    printf("%d",a);
}

