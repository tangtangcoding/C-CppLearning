#include <stdio.h>
#include <string.h>

bool containsDuplicate(int* nums, int numsSize) {
    for (int i=0;i<numsSize;++i) {
        printf("%")
    }
}

int main()
{
    char str[1000];
    int a=0,i=0;

    while(scanf("%s",str) != EOF)
    {}
    a=strlen(str);
    printf("%d",a);
}

