#include "stdio.h"

typedef struct {
    int index;
    int val;
} KeyVal;

int cmp(const void* e1, const void* e2) {
    return 
}

int main() {
    int n;

    while (scanf("%d", &n) != EOF) {
        KeyVal tmp[n], t;
        int i, j;

    }
}

