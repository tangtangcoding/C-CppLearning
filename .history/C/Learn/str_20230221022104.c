#include<stdio.h>
#include<string.h>
int main(int argc, char const *argv[])
{
    char str[5001] = {0};
    int length = 0;
    fgets(str,sizeof(str),stdin);
    const char* p = str;
    printf("%c\n")
   
    return 0;
}
